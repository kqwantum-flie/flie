<script>
  <%# todo: move this somewhere better %>
  document.onreadystatechange = function() {
    console.debug("document.readyState: " + document.readyState);
    <%# if (document.readyState == "interactive") { } %>
    if (document.readyState == "complete") {
      var oc = document.getElementById("out_current");
      if (oc != null) {
        oc.scrollIntoView();
      }

      var frame = document.getElementById("frame");
      var font_px = parseFloat(getComputedStyle(frame).fontSize);
      var width = parseInt(window.innerWidth / (font_px / 1.82));
      var width_input = document.getElementById("screen_width");
      width_input.innerText = width;
      width_input.innerHtml = width;
      width_input.value = width;

      console.log("set width to: " + width);
    }
  };
</script>
<% ps1 = ">[#{flie_o.class.name}::#{You.name}]>" %>
<% display_ps1 = ps1 %>
<% if current_user.present? %>
  <% display_ps1 = ">[#{current_user.is_eamdc? ? Rails.application.credentials.dig(:aos, :root_youser) : current_user.email_address}]>" %>
<% end %>
<div id="tty" class="bg-black text-green overflow-x-hidden">
  <div id="frame" class="flex flex-col overflow-y-auto w-screen h-screen text-xl font-semibold font-mono selection:bg-green selection:text-black">
    <div id="output" class="flex flex-col-reverse grow">
      <% if flie_o.os_logs.any? %>
        <% if flie_o.os_logs.last.out.present? %>
          <%= tag.pre(flie_o.os_logs.last.out.to_plain_text, style: "white-space: pre-wrap;") -%>
        <% end %>
        <% if flie_o.os_logs.last.in.present? %>
          <%= tag.pre("#{display_ps1}: #{flie_o.os_logs.last.in&.strip}", style: "white-space: pre-wrap;") -%>
        <% end %>
      <% end %>
    </div>
    <div class="flex grow-none flex-row">
      <div class="ps1">
        <% input_type = :text %>
        <% os_do = flie_o.os_dos.find_by(status: :active) %>
        <% if os_do.present? %>
          <% input_type = os_do.os_get.input_type %>
          <%= "#{os_do.os_get.prompt}:" %>
        <% else %>
          <%= "#{display_ps1}:$" %>
        <% end %>
      </div>

      <%= form_with url: flie_o_os_logs_path(flie_o.id), html: {class: "flex grow ml-[1rem]"} do |form| %>
        <%= form.hidden_field :screen_width %>
        <input type="<%= input_type %>" id="os_log_in" name="os_log[in]" class="grow focus:outline-none" spellcheck="false" autocomplete="off" autofocus></input>
      <% end %>

    </div>
  </div>
</div>
